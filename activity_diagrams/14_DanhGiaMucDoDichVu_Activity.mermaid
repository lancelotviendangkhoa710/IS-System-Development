flowchart TD
    subgraph Nguoi_dung [Người dùng]
        ND1("Chọn Đánh giá dịch vụ")
        ND2("Chọn kỳ đánh giá tuần/tháng/quý")
        ND3("Xem điểm đánh giá tổng hợp")
    end

    subgraph He_thong [Hệ thống]
        start(( )) --> ND1
        ND1 --> ND2
        ND2 --> HT1("Thu thập chỉ số doanh thu và khiếu nại")
        HT1 --> DK1{"Có dữ liệu phản hồi?"}
        DK1 -->|Không| HT2("Tính toán dựa trên chỉ số tài chính")
        DK1 -->|Có| HT2
        HT2 --> HT3("Hiển thị điểm đánh giá tổng hợp")
        HT3 --> ND3
        ND3 --> end_node((( )))
    end

    subgraph CSDL [CSDL]
        DB1("SELECT HOADON, phản hồi")
        Table1["HOADON"]
    end

    HT1 --> DB1
    DB1 --> Table1
    Table1 --> DK1
