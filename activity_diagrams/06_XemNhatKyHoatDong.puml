@startuml
skinparam ranksep 80
skinparam swimlaneWidth 140
skinparam shadowing false
skinparam ActivityShape rounded
skinparam linetype ortho

|Người dùng|
|Hệ thống|
|CSDL|

|Người dùng|
start
:Chọn chức năng xem nhật ký\ntrên trình đơn hệ thống;
|Hệ thống|
|CSDL|
:Truy xuất bảng danh sách\ncác sự kiện thao tác;
|Hệ thống|
if (Mất kết nối với\nmáy chủ lưu trữ?) then (Có)
  :Hiển thị thông báo lỗi gián đoạn\nvà yêu cầu thử lại sau;
  stop
else (Kết nối bình thường)
  :Hiển thị bảng danh sách các\nsự kiện thao tác từ mới nhất\nđến cũ nhất;
  |Người dùng|
  :Thiết lập mốc thời gian,\nloại hành động hoặc nhập tên nhân viên\nđể lọc dữ liệu;
  |Hệ thống|
  :Truy xuất bảng ghi vết\nvà hiển thị kết quả;
  |Người dùng|
  if (Nhấn nút xuất báo cáo?) then (Có)
    |Hệ thống|
    :Tải danh sách này về máy tính\ndưới dạng tập tin lưu trữ;
    stop
  else (Không)
    stop
  endif
endif
@enduml
